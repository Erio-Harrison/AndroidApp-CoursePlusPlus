<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group
        android:checkableBehavior="single">
        <item android:id="@+id/sort_by_enrol"
            android:title="@string/sort_by_enrol_date"
            android:checked="true" />
        <item android:id="@+id/sort_by_helpful"
            android:title="@string/sort_by_helpful" />
        <item android:id="@+id/sort_by_posted"
            android:title="@string/sort_by_posted" />
    </group>
</menu>